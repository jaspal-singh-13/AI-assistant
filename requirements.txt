# Streamlit Cloud-compatible dependencies.
# Excludes packages that require C++ compilation or are GPU/local-only.
#
# For local development with GPU inference, evals, and deploy tooling:
#   pip install -r requirements-local.txt

# Agent core
langchain>=0.3
langchain-anthropic>=0.3
langchain-openai>=0.1
langchain-huggingface>=0.1
langgraph>=0.2

# Observability
langfuse>=2.0
langsmith>=0.1

# Evals
deepeval>=1.4

# Guardrails
# presidio-analyzer/anonymizer excluded — requires 400MB spaCy model download
# that fails on Streamlit Cloud. _detect_pii() degrades gracefully to [] anyway.
filelock>=3.0

# Tools
ddgs>=6.0
requests>=2.31

# UI
streamlit>=1.35

# Utilities
python-dotenv>=1.0
nest_asyncio>=1.6
matplotlib>=3.8
pandas>=2.0
