# NeMo Guardrails configuration
# FR-GRD-04: Conversation-level declarative constraints

models:
  - type: main
    engine: langchain
    parameters:
      model_name: ChatAnthropic
      model: claude-haiku-4-5-20251001

rails:
  input:
    flows:
      - check jailbreak
      - check identity claim
  output:
    flows:
      - check illegal activity
      - check professional advice
      - check off topic
